name: Deploy to Bluehost Staging

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    name: Deploy to Bluehost via FTP
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.STAGE_FTP_SERVER }}
          username: ${{ secrets.STAGE_FTP_USERNAME }}
          password: ${{ secrets.STAGE_FTP_PASSWORD }}
          local-dir: ./web/
          server-dir: ${{ secrets.STAGE_FTP_SERVER_DIR }}
          protocol: ftps
